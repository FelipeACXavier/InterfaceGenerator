syntax = "proto3";

package dti;

import "protobuf/info.proto";

message MModelInfo {
  repeated MInfo inputs     = 1;
  repeated MInfo outputs    = 2;
  repeated MInfo parameters = 3;

  // Must be set to true by the client
  bool           request    = 10;
}