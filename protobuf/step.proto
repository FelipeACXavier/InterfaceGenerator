syntax = "proto3";

package dti;

import "protobuf/utils.proto";

message MStep {
    // Any is used since different engines use different data types
    MNumber64 step        = 1;
    MNumber64 micro_step  = 2;
}